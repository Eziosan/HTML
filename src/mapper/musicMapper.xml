<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mini.dao.MusicMapper">
	
	<!-- 가사로 노래 검색. like쓸 때 밑에 처럼 써야함  -->
	<select id="musicByLyric" parameterType="string" resultType="song">
		select * from song
		where lyrics like '%' || #{lyrics} || '%'
	</select>
	
	<!-- 검색된 노래중에 보고 싶은 노래의 id를 입력받아 찾음 -->
	<select id="showSong" parameterType="int" resultType="song">
		select * from song 
		where song_id = #{song_id}
	</select>
	
	<insert id="enroll" parameterType="song">
		insert into song (song_id, song_name, release_date, writer, composer, lyrics)
		values (song_seq.nextval , #{song_name}, #{release_date}, #{writer}, #{composer}, #{lyrics} )
	</insert>

	
	<!-- 
	<select id="selectStaff" parameterType="string" resultType="staff">
		select p.ssn, p.name, p.age, s.dept 
		from person p, staff s
		where p.ssn = s.ssn
		and p.ssn = #{ssn}
	</select>

	<insert id="insertPerson" parameterType="person">
		insert into person values(#{ssn}, #{name}, #{age}, #{type})
	</insert>
	
	
	<update id="updatePerson" parameterType="person">
		update person set name = #{name}, age = #{age} where ssn = #{ssn} 
	</update>
	
	
	
	<delete id="deletePerson" parameterType="string">
		delete from person where ssn = #{ssn}
	</delete> -->

</mapper>